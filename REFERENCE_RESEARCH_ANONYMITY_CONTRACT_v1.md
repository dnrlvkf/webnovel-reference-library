# REFERENCE_RESEARCH_ANONYMITY_CONTRACT_v1

## 목적

작품 익명성은 출처를 잊기 위한 장치가 아니다. 유명 작품의 평판·장르 선입견·고유 표면보다 작동 사슬을 먼저 읽고, 집필 참고층에서 원작 표면을 복제할 위험을 낮추기 위한 장치다.

## 세 층의 신원 분리

### 봉인 신원층

실제 작품명, 저자 표기, 원천 파일명과 묶음 내부 경로는 `registry/private/work_identity_map.yaml`에만 둔다. 원문 재검증·체크섬 확인·REF 중복 방지에만 사용한다. 일반 연구 파일, 인덱스, 영수증, 커밋 메시지에 복사하지 않는다.

### 근거 위치층

일반 연구 파일은 작품명 대신 `REF-XX`, 원천 파일명 대신 `SRC-...`를 사용한다. 원문 재진입은 source ID, 원문 SHA-256, 회차·블록, 시작·종료 행, 구간 SHA-256으로 보장한다.

### 파생 연구층

작품 모델, Macro, Micro, TH, 비교, 실행 후보에서는 작품·저자·인물·조직·지명·기술의 고유명을 숨긴다. 대신 `주인공 A`, `초기 스승 A`, `조직 A`, `단일 대상 제거 능력`처럼 기능과 관계를 보존한다.

## 기본 강도

기본값은 `sealed`다. 일반 연구층에는 REF/SRC 코드만 존재한다. 실제 식별자를 허용하는 위치는 봉인 신원 맵, 원문 자체, 원천 검증 중인 일시적 작업 메모뿐이다.

## 금지 위치

- `works/` 아래 일반 연구 파일
- 작품·장면·TH·Macro·Micro 인덱스
- 연구 영수증과 감사 파일
- 비교·MC 후보
- 커밋 메시지와 PR 제목
- 기본 종료 보고

## 재식별 방지 감사

커밋 전에 다음을 확인한다.

- 실제 작품명·저자명·원천 파일명이 일반 경로에 없는가
- 인물·조직·기술의 고유명이 작품 모델과 TH에 남지 않았는가
- 독특한 원문 문장이 검색 가능한 길이로 복사되지 않았는가
- 익명화로 관계·직위·혈연·권리 차이가 사라지지 않았는가
- source ID와 체크섬으로 원문을 다시 찾을 수 있는가

누출이 발견되면 `identity_leak`로 두고 완료 처리하지 않는다.

## REF 결합

새 원천은 먼저 SRC ID를 받는다. 기존 REF와 동일 작품인지 원문 해시·경계·봉인 신원 맵으로 확인한 뒤에만 결합한다. 제목이나 회차 수만으로 같은 작품이라 확정하지 않는다. 판본이 다르면 같은 REF 아래 별도 SRC로 둔다.

> 실제 신원은 원문으로 돌아가기 위해 봉인한다. 일반 연구 표면은 작가의 판단과 작동 사슬을 먼저 읽기 위해 익명화한다.