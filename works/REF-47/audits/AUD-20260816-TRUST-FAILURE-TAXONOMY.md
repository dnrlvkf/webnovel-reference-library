# AUD-20260816-TRUST-FAILURE-TAXONOMY

## 범위

- work: `REF-47`
- mode: `작품 전체 왕복 채굴 / 신뢰·판단권 실패 분류`
- BASE SHA: `d0374552fd2625e786fc799df69ea2680ed06e09`
- source: `SRC-DIRECT-001`
- reread: ep247,250-253,267,287,293,297,323-324,331-332,335,339,342-343 + 1-350 역검색

## 정본·원문 검증

- canonical branch `main` / BASE HEAD 직접 확인.
- Project Source standard files filename / SHA-256 / byte size lock `PASS`.
- raw source SHA-256 `95d22f155d582ec702142b906161f4b36241627a76cdf858ff9b39cd6d686a4e` / bytes `5927798` / UTF-16 `VERIFIED_MATCH`.
- 신규 대상 회차는 verified 167-350 header 범위 안에서 전체 경계와 normalized SHA를 재현했다.

## 실패 분류 감사

- `PASS`: ep323의 스승 판정을 작품 객관적 정답으로 승격하지 않음. 확인 가능한 것은 현재 방법에 대한 판단 불일치와 신뢰 유지다.
- `PASS`: ep250의 장기 반복 사망을 곧바로 독립 행위자 A의 판단 실패로 부르지 않음. 실행·통제 실패로 분리.
- `PASS`: ep251에서 같은 인물이 실패 이후 새 해석을 제공하는 후속 가치 확인.
- `PASS`: ep267의 과거 제작·납품 잘못과 현재 교정 책임을 함께 보존.
- `PASS`: ep297의 역할 기준 오독 방향을 통치자 A → 주인공 A로 고정. 반대 방향으로 뒤집지 않음.
- `PASS / HOLD`: 1-350 역검색에서 주인공 A → 상대 방향의 명시적 역할 기준 오독·위임 철회 사례를 찾지 못했으며 negative result로 보존.

## 여섯 트랙 품질 감사

- CHARACTER: `자율성을 존중한다` 같은 성격 형용사로 압축하지 않고, 현재 판단 부정과 역할 주체 신뢰의 분리를 기록. `PASS`.
- RELATIONSHIP: 신뢰·배신감·실망을 감정량으로 쓰지 않고 판단권·처벌·생사 위임·반론권·교정 책임으로 기록. `PASS`.
- EVENT: 반복 사망·재해석·관계 회수·교정 행동이 실제 상태를 바꾸는 경우만 Source Scene화. `PASS`.
- STORY: 신뢰 시험→장기 실패→재해석→회수, 처벌→자기 오독 고백의 제시 순서를 분리해 기록. `PASS`.
- PROSE: 짧은 표면 문구를 독립 공식으로 저장하지 않음. 신규 PSE/PVAR 생성 억제. `PASS`.
- TECHNIQUE: 기존 TH-02의 질문을 실패 판별까지 확장하되 새 TH를 중복 생성하지 않음. `PASS`.

## Source Scene 감사

- `SC-REF47-0024`: 기존 장면의 앞쪽 판단 충돌이 핵심 질문에 필요해 동일 장면 범위를 172849-173025로 확장. 신규 중복 ID를 만들지 않음.
- `SC-REF47-0028`: 과거 판단 비판 + 생사 재위임.
- `SC-REF47-0029`: 위임 뒤 장기 실행·통제 실패.
- `SC-REF47-0030`: 실패 뒤 독립 해석의 정보 가치.
- `SC-REF47-0031`: 적대 재평가와 제거 아닌 새 역할 제안.
- `SC-REF47-0032`: 과거 잘못 + 현재 교정 책임.
- `SC-REF47-0033`: 상대 역할 기준을 감정적 동일성으로 투영한 오독.
- 모든 신규/확장 SC는 source ID / episode / line / normalized SHA로 재진입 가능. `PASS`.

## 반례·강등 감사

- `위임 뒤 나쁜 결과 = 신뢰 실패`: `CONTRADICTED` as sufficient rule.
- `상대를 믿으면 현재 판단도 승인한다`: `CONTRADICTED` by ep323.
- `과거 잘못이 있으면 이후 판단권을 다시 맡길 수 없다`: `CONTRADICTED` by ep247/267.
- `역할 기준 오독은 상대가 실제로 잘못했을 때만 생긴다`: `CONTRADICTED`; ep297에서 합리적 판단 인정과 배신감이 병존.
- `주인공 A가 상대 역할을 잘못 읽은 명시 사례가 있다`: 현재 `HOLD`; 원문 1-350에서 직접 확인하지 못함.

## 익명성·저장 억제 감사

- 일반 연구층에 실제 작품명·저자명·고유명·검색 가능한 원문 장문을 신규 복제하지 않음.
- 원문은 source ID / episode / line / SHA로 재진입.
- 신규 Macro/Micro/PSE/PVAR/CHR/EVT/STORY/TH 없음.
- 기존 기록을 보강할 수 있는 곳은 신규 파일로 중복하지 않음.
- result: `PASS`.

## 판정

- TH-02는 성공 사례뿐 아니라 실패 판별 경계를 가진 스레드로 보강 가능하다.
- 핵심은 `상대가 항상 옳을 것`이 아니라 `상대가 역할 기준대로 판단할 권리를 유지할 것`이며, 실패 시 무엇이 틀렸는지의 층위를 먼저 판정해야 한다.
- 주인공-side 역할 기준 오독은 현재 source boundary에서 미확인이므로 다음 연구로 넘긴다.
