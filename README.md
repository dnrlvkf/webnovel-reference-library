# 웹소설 참고작 정본 연구 저장소

참고작 원문을 직접 재검증하며 CHARACTER·RELATIONSHIP·EVENT·STORY·PROSE·TECHNIQUE 여섯 트랙과 원천 장면·Macro·Micro·TH를 누적하는 비공개 연구 저장소입니다.

## 연구 시작점

1. 최신 `REFERENCE_RESEARCH_ANALYSIS_SOP`
2. `REFERENCE_RESEARCH_REPOSITORY_CONTRACT`
3. `REFERENCE_WORK_MODEL_SCHEMA`
4. `REFERENCE_RESEARCH_ANONYMITY_CONTRACT`
5. 대상 `REF`의 작품 모델·원천 장면·TH·최근 연구 영수증

## 공통 배치 운영

10화 고정 배치를 전역 기본값으로 사용하지 않습니다. 모든 참고작은 **적응형 대구간 순회 + 변화 밀집 구간 정밀 재독 + 완독 후 작품 전체 왕복 채굴**을 기본 운영으로 합니다.

- 기본 원문 순회 단위는 `40~80화`입니다.
- 대구간에 포함된 원문은 전부 직접 읽습니다. 속도 향상은 원문을 건너뛰는 방식이 아니라 반복되는 저변화 장면의 기록 밀도를 낮추는 방식으로 얻습니다.
- 캐릭터 판단 기준의 변경·오판·복귀, 관계 권리 이동, 사건 인과 전환, 기존 TH의 변형·실패·비용·반례, 장기 Payoff, POV·정보 접근권·대사·지문 변주가 발생한 구간만 더 좁게 확대 재독합니다.
- 같은 메커니즘의 반복은 기존 작품 모델·TH에 좌표와 변형을 보강하고, 회차 수에 비례해 신규 Source Scene·Macro·Micro·TH를 만들지 않습니다.
- Macro는 독립 재독 가치가 있을 때만, Micro는 Macro로 사라지는 결정적 표현이 있을 때만 만듭니다.
- 유사 사례만 반복되면 신규 등록보다 포화·반례 탐색을 우선합니다.
- 1차 완독 뒤에는 다시 10화 단위로 재주행하지 않고 미확인 질문과 활성 TH를 들고 초반·중반·후반을 왕복합니다.

배치 크기는 운영 단위이며 SOP의 공식 연구 모드를 대체하지 않습니다. 작품별 인덱스가 더 구체적인 배치 범위를 지정하면 해당 인덱스를 따릅니다.

## 집필 직전 표현 검색

참고작을 실제 집필에 사용할 때는 먼저 **대상 작품의 승인 문체·시점 계약과 최근 확정 원고**를 native anchor로 읽습니다. 그 다음 사건·스토리 문제와 장면 표현 문제를 분리합니다.

- 사건·스토리 문제: EVENT·STORY·RELATIONSHIP·CHARACTER·TH에서 조회
- 장면 표현 문제: `indexes/expression_retrieval.md`에 따라 POV·정보권·서술 거리 호환성을 먼저 검사한 뒤 PROSE·Macro·Micro·표현 장면을 조회
- 구조 참고 REF와 표현 참고 REF는 자동으로 같다고 보지 않음
- 같은 REF 우선은 표현 호환성 게이트를 통과한 후보들 사이에서만 적용
- 가능하면 대표·대비·실패/과잉 사례를 함께 읽고 원문으로 재진입
- 외부 참고작은 대상 작품의 기본 목소리를 대신하지 않음
- 집필 직전에는 현재 장면에 필요한 판단만 임시 표현 패킷으로 압축하고 외부 표현 사례는 기본적으로 1~3개만 남김
- 참고작을 닫은 뒤 대상 작품의 캐릭터 판단·관계·직업 감각·문체 계약으로 새 표현을 작성
- 초고 후 POV 계약 위반·외부 문체 혼입·의미 선점·중복 해설·주제 설명 대사·설명성 화말을 감사

표현 facet은 새 기법 ID가 아니라 검색 질문을 만드는 좌표입니다. 기존 구조의 반복 검색 실패가 확인되기 전에는 별도 표현 연구 스키마를 만들지 않습니다.

## 작품 익명성

신규 일반 연구 파일·인덱스·영수증·커밋에는 실제 작품명·저자명·인물명·조직명·기술명을 쓰지 않습니다. 작품은 `REF`, 원천은 `SRC` 코드로 식별합니다. 실제 작품명과 원천 파일명은 비공개 봉인 신원 맵에만 두고, 일반 연구층은 원천 행 범위와 SHA-256으로 다시 원문에 진입합니다.

## 현재 연구 진행

- 대상: `REF-02`
- 원천: `SRC-COL2-027`
- 정본 완료 범위: `1~60화`
- 다음 적응형 순회: `61~120화`
- 공식 모드: `구간 정밀 분석`
- 세부 범위·미확인 질문은 `works/REF-02/indexes/research.md`를 정본으로 사용합니다.

## 레거시 경계

- `history/`는 Notion 내보내기 원형과 입력 자료를 보존합니다.
- `catalog/`는 과거 이관 색인이며 일부 실제 식별자가 남아 있는 **익명화 미완료 레거시층**입니다.
- 신규 연구와 종료 보고는 `works/`의 익명 연구 파일과 연구 영수증을 기준으로 판정합니다.
- 레거시층의 작품명·고유명은 신규 연구 파일이나 커밋 메시지로 복사하지 않습니다.

참고작의 문장·고유명·사건 배열을 결과물에 그대로 복제하지 않습니다.
