# 공용 참고작 라이브러리 작업 규칙

1. 이 저장소는 참고·연구 자료이며 작품별 정본이 아니다.
2. 검색은 `catalog/`에서 시작하고, 판단 근거가 필요할 때 해당 REF의 증거·원문 위치로 내려간다.
3. 실제 집필 직전에는 **대상 작품 문체·시점 계약 → 사건·스토리 문제 → 장면 표현 문제**를 분리해서 진단한다.
4. 장면 표현 문제는 `indexes/expression_retrieval.md`의 순서를 따른다. 대상 작품의 승인 문체 프로필과 최근 확정 원고를 native anchor로 먼저 읽고, 외부 후보는 POV·정보권·서술 거리 호환성을 통과한 뒤 PROSE·Macro·Micro·표현 장면을 조회한다.
5. 구조 참고 REF와 표현 참고 REF를 자동으로 같게 두지 않는다. 같은 REF 우선은 표현 호환성 게이트를 통과한 후보들 사이에서만 적용한다.
6. 가능하면 대표·대비·실패/과잉 사례를 함께 읽은 뒤 원문으로 재진입한다.
7. 참고작의 문장, 고유명, 사건 배열을 결과물에 그대로 복제하지 않는다.
8. 구조·기능·실패 조건을 추출하되 적용 여부는 대상 작품의 정본과 연속성을 기준으로 다시 판단한다.
9. 표현 facet은 검색 질의어이지 새 기법 ID나 태그 DB가 아니다. 기존 필드로 반복 검색 실패가 확인되기 전에는 스키마를 늘리지 않는다.
10. 집필 직전에는 현재 장면에 필요한 판단만 임시 표현 패킷으로 압축하고, 외부 표현 사례는 기본적으로 1~3개만 남긴다.
11. 초고 후에는 POV 계약 위반, 외부 문체 혼입, 서술자의 의미 선점, 행동 뒤 중복 해설, 상대 없이도 성립하는 주제 설명 대사, 답하지 않은 잔여를 없애는 완결 선언, 선택과 연결되지 않는 설명 문단을 감사한다.
12. `history/`의 원형 파일은 수정하지 않는다.
13. 새 자료는 원본 보존, 체크섬, 색인 갱신을 한 커밋에서 함께 처리한다.
