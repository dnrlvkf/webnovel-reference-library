# 공용 참고작 라이브러리 작업 규칙

1. 이 저장소는 참고·연구 자료이며 작품별 정본이 아니다.
2. 신규 연구와 집필 검색은 `REPOSITORY_MANIFEST.yaml`이 가리키는 현행 계약·인덱스와 `works/REF-XX/indexes/`에서 시작한다. `catalog/`는 과거 이관 색인인 레거시 보조층이며 신규 판단의 실행 권위가 아니다.
3. 작품의 첫 진입, 신규 인물·관계·조직·공간·직업·능력·규칙의 첫 제시 또는 재온보딩을 연구할 때는 `indexes/initial_onboarding_research.md`를 먼저 적용한다. 좋은 1화의 공통 공식을 찾지 말고, 각 REF에서 독자가 처음 무엇을 학습해야 했으며 그 문제를 여섯 트랙이 어떻게 해결했는지 독립적으로 기록한 뒤 작품 간 비교로 넘어간다.
4. 실제 집필 직전에는 **대상 작품 문체·시점 계약 → 사건·스토리 문제 → 장면 결합 문제 → 장면 표현 문제 → 필요 시 산문 실현 문제**를 분리해서 진단한다.
5. Source Scene을 집필에 사용할 때는 `indexes/scene_retrieval_contract.md`를 먼저 적용한다. Source Scene의 실제 beat 사슬은 `observed_chain`이지 `recommended_chain`이 아니며, 장면 전체 일치를 요구하거나 성공 사례의 beat 수·순서·질문권·명령권·침묵·효과음 조합을 현재 장면의 기본 뼈대로 삼지 않는다. 후보마다 `matched_problem / mismatch_boundary / usable_judgment / do_not_import`를 확인한다.
6. 장면 전체의 표현 문제는 `indexes/expression_retrieval.md`의 순서를 따른다. 대상 작품의 승인 문체 프로필과 최근 확정 원고를 native anchor로 먼저 읽고, 외부 후보는 POV·정보권·서술 거리 호환성을 통과한 뒤 PROSE·Macro·Micro·표현 장면을 조회한다.
7. 문장·지문·문단의 실제 실현 문제가 핵심이면 `indexes/prose_realization_retrieval.md`를 추가로 사용한다. PVAR·PSE·Source Scene은 **어떤 원문 회차를 다시 읽을지 찾는 좌표**로 사용하고, 표면 선택은 해당 회차 전체와 필요한 인접 회차를 직접 재독한 뒤 판정한다.
8. PSE/PVAR는 PROSE의 저수준 증거·비교층이지 추천 문형·어미·태그 DB가 아니다. `~다` 반복 회피, 현재형 섞기, 접속 어미 순환, 고정 모바일 글자 수를 실행 규칙으로 만들지 않는다.
9. 표현 품질이 걸린 집필·개작에서는 **파생 기록만 읽고 산문을 생성하지 않는다.** PROSE/PSE/PVAR/Source Scene/Macro/Micro/TH/표현 facet/임시 패킷의 추상 요약은 원문 재진입 좌표이며, 실제 문장·대사·지문 선택의 실행 원천이 아니다.
10. 한 REF의 한 회차만으로 작품의 기본 표현법을 확정하지 않는다. 현재 문제와 가까운 복수 회차에서 변형·예외·복귀를 확인하고, 다른 REF를 함께 참고할 때는 각 작품을 먼저 독립적으로 직접 재독한 뒤 비교한다. 고정 개수 할당은 만들지 않는다.
11. 회차 전체 재독에서는 대사·지문·내면·설명·행동·효과음·UI·문단·문장을 따로 세는 데서 끝내지 않는다. 이 채널들이 회차 안에서 어떤 순서와 밀도로 교대하고, 각 구간에서 어휘·문법·생략·절 결합·문장 경계가 어떻게 달라지는지 함께 본다.
12. 짧은 문단과 짧은 문장을 동일시하지 않는다. `사고 단위 ≠ 문장 단위 ≠ 문단 단위 ≠ 화면 블록`을 유지하며, 하나의 판단·운동 사슬을 신체 부위나 기능 체크리스트처럼 자동 단문화하지 않는다.
13. 구조 참고 REF와 표현 참고 REF를 자동으로 같게 두지 않는다. 같은 REF 우선은 표현 호환성 게이트를 통과한 후보들 사이에서만 적용한다.
14. 가능하면 대표·대비·실패/과잉 사례를 함께 읽고, 같은 표면만 반복되면 더 수집하지 말고 다른 실현·반례·조건 차이를 찾는다.
15. 집필 직전 임시 표현 패킷은 `원문 좌표 / 호환성 / 불일치 경계 / 주의사항`을 좁히는 메모다. 패킷의 추상 결론만 남긴 채 참고 원문을 닫고 그 결론에서 문장을 복원하지 않는다.
16. 참고작의 문장, 고유명, 말버릇, 사건 배열을 결과물에 그대로 복제하지 않는다. 원문 직접 재독은 표면 복사가 아니라 추상화 손실을 막고 현재 작품의 새 표현을 선택하기 위한 것이다.
17. 초고 후에는 `indexes/scene_retrieval_contract.md`의 결합 강제 감사와 함께 POV 계약 위반, 외부 문체 혼입, 서술자의 의미 선점, 행동 뒤 중복 해설, 상대 없이도 성립하는 주제 설명 대사, 답하지 않은 잔여를 없애는 완결 선언, 선택과 연결되지 않는 설명 문단, 기계적 시제·어미 다양화, 이유 없는 연속 단문화를 감사한다.
18. `catalog/`의 `execution_structure`, `completion_conditions`, `recommended_combinations` 등 레거시 실행 카드 필드는 과거 운용 흔적이다. 현행 계약과 원문 재검증 없이 집필 명령·자동 발동 규칙·권장 장면 뼈대로 사용하지 않는다.
19. `history/`의 원형 파일은 수정하지 않는다.
20. 새 자료는 원본 보존, 체크섬, 색인 갱신을 한 변경 묶음에서 함께 처리한다.
